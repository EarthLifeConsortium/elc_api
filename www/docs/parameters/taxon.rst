`taxon=taxon_name|genus+species`
    A single name at any taxonomic level or a two word pair refering to a single species. As spaces are not allowed in HTTP URLs, the taxon names must be connected by a "+" symbol or a "%20" escape sequence. Type: ``str``.
